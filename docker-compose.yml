services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PORT: 8000
      FRAME_WORKERS: 4
    volumes:
      - ./data:/app/data
    restart: unless-stopped
